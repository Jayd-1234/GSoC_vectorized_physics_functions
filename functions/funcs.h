#ifndef FUNCS_H
#define FUNCS_H

#endif // FUNCS_H

void myratio(float* num, float* den, float* out, int len);
void deltaR2(float* eta1, float* phi1, float* eta2, float* phi2, float* out, int len);
void deltaphi(float* phi1, float* phi2, int len);

